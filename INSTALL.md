# Setup development environment

1. Install prerequisites:

```bash
make setup
```

2. Build the PDF resume from `README.md` into `build/`:

```bash
make build-pdf
```

3. Build the HTML resume from `README.md` into `build/`:

```bash
make build-html
```

4. Build both release artifacts:

```bash
make build-all
```
